package org.hatchery.testArena;

// import org.springframework.ai.chat.prompt.Prompt;
// import org.springframework.ai.chat.prompt.PromptTemplate;
import java.util.Map;

public class SpringAIExample {
    public void generatePrompt() {
        // Define template with {brackets}
        // String templateString = "Tell me a {adjective} joke about {topic}.";
        
        // // Create template and inject variables
        // PromptTemplate promptTemplate = new PromptTemplate(templateString);
        // Prompt prompt = promptTemplate.create(Map.of(
        //     "adjective", "funny",
        //     "topic", "software engineering"
        // ));
        
        // // The Prompt object can now be passed to a ChatClient
        // String renderedContent = prompt.getContents();
    }
}

